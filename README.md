<p align="center">
  <img src="https://img.shields.io/badge/Go-1.23+-00ADD8?style=flat-square&logo=go" />
  <img src="https://img.shields.io/badge/Wails-v2-412991?style=flat-square" />
  <img src="https://img.shields.io/badge/platform-macOS%20%7C%20Linux%20%7C%20Windows-lightgrey?style=flat-square" />
  <img src="https://img.shields.io/badge/license-MIT-green?style=flat-square" />
</p>

# 🌿 Canopy

**Локальный десктопный дашборд для разработчиков.** Все репозитории, PR, CI/CD, issues и алерты — в одном нативном окне. Без браузера, без подписок, без телеметрии.

---

## Проблема

У каждого разработчика есть 5–20 репозиториев, разбросанных между GitHub, GitLab и Bitbucket. Чтобы понять, что происходит прямо сейчас, приходится:

- Открыть GitHub → проверить PR → переключиться на другой репо → проверить CI
- Зайти в Actions → найти упавший билд → понять в каком репо
- Вспомнить, что тебя добавили ревьюером 3 дня назад и ты забыл
- Гадать, какие ветки пора удалить, а какие зависимости обновить

Это занимает 15–30 минут каждое утро. Canopy сжимает это до 10 секунд.

---

## Что делает Canopy

Canopy — нативное десктопное приложение на [Wails](https://wails.io/) (Go + Web frontend), которое периодически опрашивает API платформ и показывает всё в одном окне.

### Обзор на одном экране

- **Статус CI/CD** для всех репозиториев — зелёный, красный, жёлтый. Один взгляд — и ты знаешь, что сломано.
- **Pull Requests** — где ты автор или ревьюер, с чётким разделением «ждёт тебя» и «ждёт других».
- **Назначенные issues** — сгруппированные по приоритету, с лейблами (bug, feature, tech debt).
- **Таблица репозиториев** — язык, текущая ветка, последний коммит, покрытие тестами.

### Алерты и предупреждения

- Stale-ветки, которым больше N дней
- Уязвимости в зависимостях (из Dependabot / Renovate)
- Устаревшие зависимости с мажорными обновлениями
- PR без активности дольше N дней

### Релизы из приложения

- Просмотр истории релизов для каждого репо

---

## Технологии

| Компонент             | Технология          | Почему                                                                         |
| --------------------- | ------------------- | ------------------------------------------------------------------------------ |
| **Десктоп-фреймворк** | Wails v2            | Нативное окно, ~15 MB бинарник, нет Electron-раздутости                        |
| **Бэкенд**            | Go 1.26             | Быстро, один бинарник, отличные HTTP-клиенты                                   |
| **Фронтенд**          | React + TypeScript  | Лёгкий, реактивный UI                                                          |
| **Хранение**          | SQLite (go-sqlite3) | Кеш данных, мгновенный старт, zero-config                                      |
| **GitHub API**        | google/go-github    | Официальная библиотека, REST + GraphQL                                         |
| **Секреты**           | zalando/go-keyring  | Системный keychain (macOS Keychain, GNOME Keyring, Windows Credential Manager) |

---

## Архитектура

```
canopy/
├── main.go                     # Точка входа Wails
├── app.go                      # Wails bindings (Go ↔ JS)
│
├── internal/
│   ├── config/
│   │   └── config.go           # Парсинг dashboard.yaml
│   │
│   ├── provider/               # Адаптеры для платформ
│   │   ├── provider.go         # Общий интерфейс Provider
│   │   ├── github.go           # GitHub REST/GraphQL
│   │   ├── gitlab.go           # GitLab API
│   │   └── bitbucket.go        # Bitbucket API
│   │
│   ├── poller/
│   │   └── poller.go           # Фоновый опрос API по таймеру
│   │
│   ├── store/
│   │   ├── sqlite.go           # SQLite кеш
│   │   └── models.go           # Repo, PR, CIRun, Issue, Alert
│   │
│   └── keyring/
│       └── keyring.go          # Обёртка над go-keyring для токенов
│
├── frontend/                   # React приложение
│   ├── src/
│   │   ├── App.tsx
│   │   ├── pages/
│   │   │   ├── Dashboard.tsx
│   │   │   ├── Repos.tsx
│   │   │   ├── PullRequests.tsx
│   │   ├── components/
│   │   │   ├── StatCard.tsx
│   │   │   ├── PRList.tsx
│   │   │   ├── CIStatus.tsx
│   │   │   └── AlertPanel.tsx
│   │   └── lib/
│   │       └── wails.ts
│   └── package.json
│
├── dashboard.yaml
├── wails.json
└── .github/
    └── workflows/
        └── release.yml
```

### Поток данных

```
GitHub/GitLab API
       │
       ▼
   [ Poller ]  ──  каждые N минут опрашивает провайдеры
       │
       ▼
   [ SQLite ]  ──  кешированные данные
       │
       ▼
   [ Wails Bindings ]  ──  Go функции, вызываемые из JS
       │
       ▼
   [ Frontend ]  ──  реактивный UI, обновляется через Events
```

Wails bindings означают, что фронтенд вызывает Go-функции напрямую — без REST API, без WebSocket-сервера. Просто `await GetDashboardData()` из JavaScript, а Wails маршрутизирует вызов в Go.

---

## Установка

### Из бинарника

Скачать последний релиз со страницы [Releases](https://github.com/username/canopy/releases):

```bash
# macOS (Apple Silicon)
curl -L https://github.com/username/canopy/releases/latest/download/canopy-darwin-arm64.tar.gz | tar xz
mv canopy /usr/local/bin/

# Linux
curl -L https://github.com/username/canopy/releases/latest/download/canopy-linux-amd64.tar.gz | tar xz
mv canopy /usr/local/bin/
```

### Из исходников

```bash
# Установить Wails CLI
go install github.com/wailsapp/wails/v2/cmd/wails@latest

# Клонировать и собрать
git clone https://github.com/username/canopy.git
cd canopy
wails build
```

### Первый запуск

```bash
# Настроить токен (сохраняется в системный keychain)
canopy auth github

# Создать конфиг
canopy init

# Запустить
canopy
```

---

## Roadmap

### v0.1.0 — MVP

- [x] GitHub интеграция (репо, PR, CI статусы)
- [ ] SQLite кеш
- [x] Дашборд с основными виджетами
- [x] Десктопное окно через Wails

### v0.2.0 — Расширение

- [ ] GitLab провайдер
- [ ] Создание релизов из UI
- [ ] Авто-генерация changelog из conventional commits
- [ ] Десктопные уведомления (CI fail, новый PR)

### v0.3.0 — Продвинутые фичи

- [ ] Bitbucket провайдер
- [ ] Интеграция с Linear / Jira для issues
- [ ] Покрытие тестами (Codecov, Coveralls)
- [ ] Графики: коммиты за неделю, время CI, тренды покрытия
- [ ] Быстрые действия: merge PR, retry CI, создать issue

### v1.0.0 — Полноценный инструмент

- [ ] Система плагинов для кастомных виджетов
- [ ] Мульти-аккаунт (рабочий + личный GitHub)
- [ ] CLI-режим (`canopy status` — вывод в терминал)
- [ ] Автообновление (Sparkle / go-update)

---

## Контрибьютинг

Canopy задуман как проект, в который легко контрибьютить. Каждый провайдер — это реализация одного интерфейса:

```go
type Provider interface {
    Name() string
    ListRepos(ctx context.Context) ([]Repo, error)
    ListPullRequests(ctx context.Context, repo string) ([]PullRequest, error)
    GetCIStatus(ctx context.Context, repo, ref string) (CIStatus, error)
    ListIssues(ctx context.Context, repo string) ([]Issue, error)
    CreateRelease(ctx context.Context, repo string, opts ReleaseOpts) (*Release, error)
}
```

Добавить новую платформу = реализовать этот интерфейс + добавить парсинг конфига.

Виджеты на фронтенде — независимые Svelte-компоненты, каждый получает данные через один Wails binding.

---

## Лицензия

MIT — используй как хочешь.
